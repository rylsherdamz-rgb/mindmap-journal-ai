# --- Web / API runtime (Cloud Run) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# --- ML model (training + inference) ---
# Pinned to the exact versions used to train model/emotion_model.joblib so the
# serialized pipeline loads without scikit-learn InconsistentVersionWarning / errors.
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.5.2
scipy==1.18.1

# --- Dataset loading for training (ml/train.py) ---
pandas==2.2.3
pyarrow==18.1.0

# --- Google Cloud / Firebase / Gemini ---
google-cloud-secret-manager==2.22.0
firebase-admin==6.6.0
google-genai==0.5.0
